import SwiftUI

//struct Info {
//    let vc = ViewController()
//    
//     
//}

struct ContentView: View {
    
    var body: some View {
        VStack {
            print(vc)
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, world!")
        }
    }
}
