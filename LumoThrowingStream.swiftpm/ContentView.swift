// LumoThrowingStream  10/05/2025-1
// CoreMotion attitude quaternion AsyncThrowingStream
// 

import SwiftUI
struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Hello, nucklehead!")
        }
        .font(.title)
    }
}
